services:
  backend:
    build: .
    container_name: alfa-hackiton-backend
    ports:
      - "5000:5000"
    environment:
      DB_PATH: /app/data/app.db
    volumes:
      - type: bind
        source: ./data
        target: /app/data
    restart: unless-stopped
