services:
  backend:
    build: .
    container_name: alfa-hackiton-backend
    ports:
      - "5000:5000"
    environment:
      DB_PATH: /app/data/app.db
      FIRECRAWL_API_KEY: ${FIRECRAWL_API_KEY:-}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
